import { propTypes as controlledInputFieldPropTypes } from '../ControlledInputField/ControlledInputField.prop'

export const propTypes = controlledInputFieldPropTypes

export const defaultProps = {
  labelIsLight: false,
  checked: false,
  inputType: 'radio',
}
