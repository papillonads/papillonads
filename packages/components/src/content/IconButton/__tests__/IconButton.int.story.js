import { storiesOf } from '@storybook/react'
import { renderDefault } from './IconButton.int.render'
import notes from '../IconButton.md'

storiesOf('Content|Molecule/IconButton', module).add('Default', renderDefault, { notes })
