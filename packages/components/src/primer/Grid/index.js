import { DisplayTable } from './DisplayTable'
import { FlexGrid, flexGridSelection, flexGridState } from './FlexGrid'

export const Grid = {
  DisplayTable,
  FlexGrid,
  flexGridSelection,
  flexGridState,
}
